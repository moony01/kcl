# KCL 프로젝트 지침

## 브랜치 전략

- **`main`**: 기본 개발 및 배포 브랜치. 모든 일반 작업은 main에서 직접 진행
- **`feature/auth`**: 로그인/인증 기능 전용 브랜치. 로그인 관련 작업만 이 브랜치에서 수행
- 로그인 외 신규 기능은 별도 브랜치 없이 main에서 개발 후 바로 배포

## 피처 플래그

- `src/config/features.ts`에서 기능 ON/OFF 제어
- **`AUTH_SYSTEM`**: 현재 `false` (로그인 UI 숨김). 로그인 개발 완료 후 `true`로 전환하여 활성화
